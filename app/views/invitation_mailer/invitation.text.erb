<%= t '.title', :email => "#{@invitation.email}" %>

<%= t '.invitedto', :user => "#{@invitation.user.username}", :appname => "#{Rails.application.name}" %> (<%= Rails.application.root_url %>)<%= @invitation.memo.present? ? raw("\n\n   #{@invitation.memo}") : "" %>

<%= t '.createaccount' %>

<%= Rails.application.root_url %>invitations/<%= @invitation.code %>
