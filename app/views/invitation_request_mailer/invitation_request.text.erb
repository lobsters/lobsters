<%= t '.title', :email => "#{@invitation_request.email}" %>

<%= t '.invitedto', :userip => "#{@invitation_request.ip_address}", :appname => "#{Rails.application.name}" %>

  <%= t '.name', :name => "#{@invitation_request.name}" %>
  <%= t '.email', :email => "#{@invitation_request.email}" %>
  <%= t '.memo', :memo => "#{@invitation_request.memo}" %>

<%= t '.warning' %>

  <%= Rails.application.root_url %>invitations/confirm/<%= @invitation_request.code %>

<%= t '.deletemessage' %>
